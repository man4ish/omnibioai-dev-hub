# Repos indexed by build_index.py.
# The actual paths are constructed as: ${REPO_BASE}/<name>
# Set REPO_BASE in your environment or .env file before running build_index.py.
#
# Local default:  REPO_BASE=/home/manish/Desktop/machine
# Docker default: REPO_BASE=/repos  (set via ENV in Dockerfile)

repos:
  - omnibioai
  - omnibioai-rag
  - omnibioai-toolserver
  - omnibioai-sdk
  - omnibioai-workflow-bundles
  - omnibioai-control-center
  - omnibioai-lims
  - omnibioai-model-registry
  - omnibioai-dev-docker
  - omnibioai-api-gateway
  - omnibioai-docs
  - omnibioai-studio
  - omnibioai-auth
  - omnibioai-tool-runtime
  - omnibioai-iam-client
  - omnibioai-policy-engine
  - omnibioai-security-audit
  - omnibioai-security-sdk
  - omnibioai-hpc-policy-engine
